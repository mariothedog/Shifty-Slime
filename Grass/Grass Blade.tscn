[gd_scene load_steps=4 format=2]

[ext_resource path="res://Grass/Grass Blade.png" type="Texture" id=1]

[sub_resource type="Shader" id=3]
code = "shader_type canvas_item;

uniform float sway_amount = 40.0;
uniform float speed = 1.5;
uniform float dir = 1.0;
uniform float colour_multi = 1.0;

void vertex() {
	if (VERTEX.y < 0.0) {
		VERTEX.x += sin(TIME * speed) * sway_amount * dir;
	}
}

void fragment() {
	COLOR = texture(TEXTURE, UV);
	COLOR *= colour_multi;
}"

[sub_resource type="ShaderMaterial" id=2]
shader = SubResource( 3 )
shader_param/sway_amount = 40.0
shader_param/speed = 1.5
shader_param/dir = 1.0
shader_param/colour_multi = 1.0

[node name="Sprite" type="Sprite"]
material = SubResource( 2 )
scale = Vector2( 0.25, 0.25 )
texture = ExtResource( 1 )
offset = Vector2( 0, -67 )
